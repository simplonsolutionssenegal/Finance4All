{
  "name": "finance4all",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "build": "npm run build:backend && npm run build:frontend",
    "build:frontend": "cd frontend && npm run build",
    "build:backend": "cd backend && npm run build",
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:frontend": "cd frontend && npm run dev",
    "dev:backend": "cd backend && npm run dev",
    "sonar": "sonar-scanner",
    "sonar:frontend": "cd frontend && sonar-scanner",
    "sonar:backend": "cd backend && sonar-scanner",
    "prepare": "husky || echo 'Husky not found, skipping hooks setup'"
  },
  "devDependencies": {
    "@types/jest": "^30.0.0",
    "concurrently": "^7.6.0",
    "cross-env": "^10.0.0",
    "cross-env": "^10.0.0",
    "eslint-config-prettier": "^10.1.8",
    "husky": "^9.1.7",
    "jest": "^30.1.2",
    "sonarqube-scanner": "^3.5.0",
    "ts-jest": "^29.4.1"
  },
  "dependencies": {
    "@clerk/nextjs": "^6.31.6",
    "@hookform/resolvers": "^5.2.1",
    "zod": "^4.1.3"
  },
  "lint-staged": {
    "frontend/**/*.{js,jsx,ts,tsx}": [
      "npm run -w frontend lint:fix",
      "npm run -w frontend format"
    ],
    "backend/**/*.{js,ts}": [
      "npm run -w backend lint:fix",
      "npm run -w backend format"
    ],
    "**/*.{json,md}": [
      "prettier --write"
    ]
  }
}
