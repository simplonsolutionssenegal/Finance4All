# Configuration NPM pour optimiser les builds Docker
# Désactive les scripts de sécurité non essentiels
fund=false
audit=false
# Scripts autorisés (liste blanche)
# unsafe-perm=true uniquement si nécessaire pour des binaires natifs