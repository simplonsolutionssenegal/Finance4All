#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

echo "🧪 Exécution des tests avant push..."

# Type checking
echo "🔍 Vérification des types..."
cd frontend && npm run type-check
if [ $? -ne 0 ]; then
  echo "❌ Erreurs de types dans le frontend"
  exit 1
fi

cd ../backend && npm run type-check
if [ $? -ne 0 ]; then
  echo "❌ Erreurs de types dans le backend"
  exit 1
fi

cd ..

# Tests
#echo "🧪 Exécution des tests backend..."
#cd backend && npm test
#if [ $? -ne 0 ]; then
#  echo "❌ Tests backend échoués"
#  exit 1
#fi
#
#echo "🧪 Exécution des tests frontend..."
#cd ../frontend && npm test -- --passWithNoTests
#if [ $? -ne 0 ]; then
#  echo "❌ Tests frontend échoués"
#  exit 1
#fi
#
#cd ..

echo "✅ Tous les tests sont passés, push autorisé!"