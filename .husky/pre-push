echo "🔍 Vérification avant push..."

# Type checking
echo "🔍 Vérification des types..."
cd frontend && npm run type-check
if [ $? -ne 0 ]; then
  echo "❌ Erreurs de types dans le frontend"
  exit 1
fi

cd ../backend && npm run type-check
if [ $? -ne 0 ]; then
  echo "❌ Erreurs de types dans le backend"
  exit 1
fi